//In NodeJS, require() is a built-in function to include external modules
// that exist in separate files. require() statement basically reads a JavaScript file,
// executes it, and then proceeds to return the export object.
// require() statement not only allows to add built-in core NodeJS modules but also
// community-based and local modules.
require("./ficheiro_1.js")
require("./ficheiro_2.js")

